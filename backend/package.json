{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "cd .. && eslint \"backend/**/*.ts\" --config ./frontend/eslint.config.js --no-warn-ignored",
    "test": "node --test ./*.test.ts ./scripts/*.test.ts",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3"
  }
}
