{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "cd .. && eslint \"backend/**/*.ts\" --config ./frontend/eslint.config.js --no-warn-ignored",
    "test": "node --test ./matchupHistoryStore.test.ts",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "better-sqlite3": "^12.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3"
  }
}
